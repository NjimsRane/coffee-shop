import { NavBarDown, NavBarTop } from '../../components';

const Header = () => {
    return (
        <div>
            <NavBarTop />
            <NavBarDown />
        </div>
    );
};

export default Header;